import React from 'react'
import HeadingOne from '../../ui/HeadingOne'

const page = () => {
  return (
    <section>
        <HeadingOne text="Projects" />
        <p>Luna Link is a fleet management tool designed to track trailers across multiple yards. Initially built using the MERN stack with EJS, the project is now transitioning to a React-based, production-ready version. It aims to streamline fleet logistics by offering intuitive tracking and management capabilities.</p>
    </section>
  )
}

export default page